set-option -g history-limit 5000

# misc
set -g status-interval 2
set -g status-utf8 on

## left
#set -g status-left "[#S]#(tmux-mem-cpu-load --colors)#[default]"
set -g status-left "[#S]#[default]"

## right
set -g status-right '#(~/bin/tmux_battery_charge_indicator.sh) #(/usr/local/bin/battery -t -w red -m black -g black) #[bg=white,fg=colour240] %H:%M #[bg=colour240,fg=white] %Y-%m-%d'

